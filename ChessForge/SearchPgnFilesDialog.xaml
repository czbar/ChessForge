<Window x:Class="ChessForge.SearchPgnFilesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:p="clr-namespace:ChessForge.Properties"
        xmlns:local="clr-namespace:ChessForge"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.SearchInPgnFiles}" Height="450" Width="540">
    <Grid>
        <Label x:Name="UiLblDirectory" Content="{x:Static p:Resources.FileFolder}" HorizontalAlignment="Right" Margin="0,20,475,0" VerticalAlignment="Top"/>
        <TextBox x:Name="UiTbDirectory" IsEnabled="False" HorizontalAlignment="Center" Margin="0,25,0,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Width="400"/>
        <Button x:Name="UiBtnSelectFolder" Content=". . ." HorizontalAlignment="Center" Margin="470,23,15,0" VerticalAlignment="Top" Width="28" />
        <Button x:Name="UiBtnStartStop" Content="{x:Static p:Resources.Search}" Width="80" HorizontalAlignment="Center" Margin="0,55,0,0" VerticalAlignment="Top"/>
        <GroupBox x:Name="UiGbProgress" Header="{x:Static p:Resources.Progress}" HorizontalAlignment="Center" Height="60" Width="400" Margin="0,80" VerticalAlignment="Top" >
            <Label x:Name="UiLblSearchProgress" Content="{x:Static p:Resources.NotStarted}" HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Top"/>
        </GroupBox>
        <ListBox x:Name="UiLbFiles" Width="400" Margin="0,160,0,85"/>
        <Button x:Name="UiBtnClose" Content="{x:Static p:Resources.Close}" Width="80" HorizontalAlignment="Center" Margin="0,0,0,45" VerticalAlignment="Bottom"/>
        <Button x:Name="UiBtnCancel" IsCancel="True" Content="" Width="0" Height="0" />
    </Grid>
</Window>
