<Window x:Class="ChessForge.OnlineLibraryContentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:p="clr-namespace:ChessForge.Properties"
        xmlns:local="clr-namespace:ChessForge"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.DlgTitleLibraryContent}" Height="550" Width="800">
    <Grid>
        <RichTextBox x:Name="UiRtbOnlineLibrary" IsReadOnly="True" Cursor="Arrow"  
                     Margin="10,10,10,50" VerticalScrollBarVisibility="Auto">
            <FlowDocument>
            </FlowDocument>
        </RichTextBox>
        <Button x:Name="UiBtnClose" IsCancel="True" Content="{x:Static p:Resources.Close}" HorizontalAlignment="Center" Margin="-120,0,0,15" VerticalAlignment="Bottom" Width="100" Click="UiBtnClose_Click" />
        <Button x:Name="UiBtnHelp" Content="{x:Static p:Resources.Help}" HorizontalAlignment="Center" Margin="120,0,0,15" VerticalAlignment="Bottom" Width="100" Click="UiBtnHelp_Click" />
        <Button x:Name="UiBtnLibraries" Content="{x:Static p:Resources.Libraries}" HorizontalAlignment="Right" Margin="0,0,40,15" VerticalAlignment="Bottom" Width="100" Click="UiBtnLibraries_Click" />
    </Grid>
</Window>
