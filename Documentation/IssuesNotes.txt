Training: evaluating line does not bring Runs into view. Also, maybe evaluate line experience should include making moves and eval chart?
  While evaluating lines remove the move prompt para and then restore it.  Stop variation eval when any move is clicked.
  Changing to earlier engine move does not change the eval node for the engine.

RTF: if everything in the paragraph is bold, set the font bold to the para instead of individual moves. Should dramatically reduce size.
 
Review checking if nodes are identical in various Paste contexts.

Unify the implementation of SCOPE constants.

IntroView uses _rtb static reference to HostRtb. This is problematic. Try text change event handling as non-static.

Speed up Undo delete move for simple cases. Same Undo for AddMove and for Delete move.

Look into that mysterious bottom margin = 5 in game layout.

When importing from multiple PGN offer an option to create a separate chapter from each file.

Test the game mode.

In Study, after some edit and CTRL+S, the view moves to highlight the selected move.

Restarting training game at an earlier move causes all sorts of confusion in the game notation, diagram and who is on the move.

In Study, no spacing after the comment under a diagram before next line. Is this intentional?

After a comment on the main line, adding comment before-move for the next move, inserts spurious leading space.

Search Position should not complain about the king in check - we don't specify who is on the move - or should we?

A means to find out if a game/exercise is referenced somewhere already.

External cache of positions so that "too many requests" can be better handled. Note: add an exclam or something to indicate that the result is stale.

Why is sound disappearing?

When adjusting reference positions, give preference to the game's text even if it is shorter than the comment line.